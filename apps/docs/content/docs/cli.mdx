---
title: CLI
description: The CLI to import hooks directly to your codebase.
---

# Installation

To use the CLI, you can execute it directly with the latest version:

```bash title="Terminal"
npx rehooks-cli@latest
```

# Usage

```bash title="Terminal"
npx rehooks-cli@latest [options] [command]
```

## Options

These are the options available in the CLI.

| Option          | Description                 |
| --------------- | --------------------------- |
| `-v, --version` | Displays the version number |
| `-h, --help`    | Displays help for command   |

## Commands

| Command          | Description                          |
| ---------------- | ------------------------------------ |
| `init`           | Initialize the Rehooks configuration |
| `add`            | Add custom hooks to your codebase.   |
| `help [command]` | Displays help for a specific command |
